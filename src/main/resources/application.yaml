bot:
  name: MegacomChatGPTbot
  token: ${bottoken}
  apitoken: ${apitoken}
  limittoken: 4000
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/chatgpt
    username: sam
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: true
    database: postgresql
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect